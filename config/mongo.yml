defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: test_app_mongodb_development

test:
  <<: *defaults
  database: test_app_mongodb_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: test_app_mongodb
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
